services:
  feed-webmvc:
    build: .
    container_name: feed-webmvc
    environment:
      - SERVER_PORT=8383
    ports:
      - "8383:8383"
    networks:
      - reactive-ms-network

networks:
  reactive-ms-network:
    external: true